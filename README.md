# WorldExplorer
React app using REST Countries API to search/filter countries &amp; view details (population, capital, flags). Features responsive CSS, session management, optional JWT auth via Node.js backend. Deployed with Git, tests, &amp; docs.
